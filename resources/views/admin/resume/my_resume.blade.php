<!DOCTYPE html>
<html>
<head>
    <title></title>
</head>
<body>
    <h1>Hello</h1>
    {{-- <h1>{{ $myresume->personal_detail->name }}</h1>
    <p>{{ $myresume->personal_detail->summary }}</p>

    <h2>Skills</h2>
    <ul>
        @foreach($myresume->skills as $skill)
            <li>{{ $skill->language }} - Level: {{ $skill->level }}</li>
        @endforeach
    </ul>

    <h2>Work Experience</h2>
    <ul>
        @foreach($myresume->work_experiences as $work)
            <li>{{ $work->company_name }} ({{ $work->start_date }} - {{ $work->end_date }})</li>
        @endforeach
    </ul>

    <h2>Education</h2>
    <ul>
        @foreach($myresume->educations as $education)
            <li>{{ $education->institution }} - {{ $education->degree }}</li>
        @endforeach
    </ul> --}}
</body>
</html>
